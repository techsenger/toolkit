name: Snapshot Deploy

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
    - uses: actions/checkout@v4
    - name: Setup Java 11
      uses: actions/setup-java@v3
      with:
        java-version: '11'
        distribution: 'temurin'
        cache: 'maven'
    - name: Generate settings.xml
      run: |
          cat > ~/.m2/settings.xml << EOF
          <settings>
            <servers>
              <server>
                <id>repsy-snapshots</id>
                <username>${{ secrets.REPSY_USERNAME }}</username>
                <password>${{ secrets.REPSY_PASSWORD }}</password>
              </server>
            </servers>
          </settings>
          EOF
    - name: Install Xvfb
      run: sudo apt-get update && sudo apt-get install -y xvfb
    - name: Deploy only snapshot versions
      run: |
        VERSION=$(mvn help:evaluate -Dexpression=project.version -q -DforceStdout)
        echo "Detected version: $VERSION"
        if [[ $VERSION == *"SNAPSHOT"* ]]; then
          echo "Deploying snapshot version: $VERSION"
          # toolkit-fx's VirtualFlow UI tests need a real (if virtual) display: TableView/ListView/TreeView
          # only get real, non-zero row heights once part of an actually shown Stage/Scene, and mvn deploy
          # runs the default profile's tests (Surefire) before publishing. xvfb-run gives the JavaFX toolkit
          # a working display without a real monitor.
          xvfb-run -a mvn deploy -U -B
        else
          echo "Skipping deploy for release version: $VERSION"
        fi
      env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
